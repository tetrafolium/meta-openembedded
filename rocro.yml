inspecode:
  tasks:
    - name: repo
      type: shell
      commands:
        - sudo cat ./artifacts_report.json
      artifacts:
        - path: ./artifacts_report.json
      reports:
        - path: ./artifacts_report.json
