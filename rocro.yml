inspecode:
  tasks:
    - name: child-repo
      type: shell
      commands:
        - ls -alt ./
        - cd ./ant
        - cov-build --dir ~/cov-im --parallel-translate=1 ant
      artifacts:
        - path: ./report.sarif
      reports:
        - path: ./report.sarif
